# Проект "HomeFlip" - Сервис обмена жильемКореник Илья 353502



**Студент:** Кореник Илья# МОДЕЛИ ДАННЫХ и СИСТЕМЫ УПРАВЛЕНИЯ БАЗАМИ ДАННЫХ

**Группа:** 353502

## Общие требования

---

1. Для выполнения лабораторных работ вы можете использовать любую **реляционную СУБД** (например, PostgreSQL, MySQL).

## 🤖 Copilot Instructions & Plan2. Для сдачи лабораторной работы необходимо создать git-репозиторий, где `README.md` содержит ваши данные (Имя, Фамилия, Номер группы).



Этот раздел предназначен для отслеживания прогресса выполнения лабораторных работ и инструкций для будущих сессий.В данном курсе вы будете проектировать базу данных в контексте приложения, предметную область которого вы определите в первой лабораторной. Все последующие лабораторные работы будут дополнять предыдущие и расширять функционал вашей БД.

В конце курса вы получите реализованную базу данных, набор SQL-запросов и инструкций, готовых для применения в коде проекта.

### Текущий статусВзаимодействие с базой данных в вашем проекте будет производиться исключительно посредством данного набора запросов. Проект должен также иметь интерфейс взаимодействия с пользователем.

- [x] **Lab 1:** Определение темы, требований и сущностей.

- [x] **Lab 2:** Нормализация и даталогическая модель.Последняя лабораторная работа представляет собой готовый мини-проект, в котором в качестве базы данных используется реализованная вами БД.

- [ ] **Lab 3:** Физическая модель, миграции, Docker, тестовые данные.

- [ ] **Lab 4:** Сложные выборки и представления.---

- [ ] **Lab 5:** Триггеры и хранимые процедуры.

- [ ] **Lab 6:** Приложение с UI.# Лабораторная работа №1



### Инструкции### Необходимо:

1. При выполнении следующей лабораторной работы, обновляйте соответствующий раздел в этом файле.

2. Все SQL скрипты должны быть идемпотентными (по возможности) и находиться в папке `scripts/`.1. Определить тему разрабатываемого проекта.

3. Поддерживайте актуальность `docker-compose.yml`.2. Определить функциональные требования к проекту.

3. Определить и обосновать перечень сущностей БД, удовлетворяющих функциональным требованиям:

---

   * Количество связанных сущностей — **не менее 10**

## 📝 Лабораторная работа №1: Инфологическая модель   * Промежуточная таблица many-to-many **не считается сущностью**, если не содержит дополнительной информации

   * Должны присутствовать **все виды связей**

### Тема проекта4. Схематично изобразить **ненормализованную схему БД**.

**HomeFlip** — сервис для обмена жильем (Home Exchange). Позволяет пользователям находить варианты для проживания в других городах и странах, предлагая взамен свое жилье.5. Описать каждую сущность (имя поля, тип, ограничения, связи).



### Функциональные требования### Acceptance Criteria:

Подробные функциональные требования описаны в файле [functional_requirements.md](functional_requirements.md).

Основные возможности:1. `README.md` содержит тему, ФИО, номер группы.

- Регистрация и аутентификация пользователей.2. Определён, описан и закоммичен файл с функциональными требованиями. Минимальные требования:

- Управление профилем и ролями.

- CRUD операции для объектов недвижимости (Accommodations).   * Авторизация/аутентификация

- Поиск и фильтрация жилья.   * Управление пользователями (CRUD)

- Система бронирования (Exchange Offers).   * Система ролей

- Отзывы и рейтинги.   * Журналирование действий

3. Определён список таблиц.

### Описание сущностей (ER-диаграмма)4. Каждая таблица описана.

5. Есть схема предлагаемой инфологической модели БД.

#### Справочники6. Теоретические знания по теме.

1. **countries** (Страны): `id`, `name`, `iso_code`.

2. **cities** (Города): `id`, `name`, `country_id`, `region`.---

3. **accommodation_types** (Типы жилья): `id`, `name` (Квартира, Дом, Лофт...), `description`.

4. **accommodation_statuses** (Статусы жилья): `id`, `code` (draft, published, archived), `name`.# Лабораторная работа №2

5. **exchange_offer_statuses** (Статусы обмена): `id`, `code` (pending, accepted, rejected), `name`.

6. **amenities** (Удобства): `id`, `short_name` (Wi-Fi, Кухня, Бассейн...).### Необходимо:



#### Основные сущности1. Разработать даталогическую модель БД.

7. **users** (Пользователи): `id`, `email`, `yandex_id`, `first_name`, `last_name`, `phone`, `role_id`.2. Привести БД к **третьей нормальной форме**.

8. **roles** (Роли): `id`, `name` (admin, user, guest), `description`.

9. **privileges** (Привилегии): `id`, `name`, `description`.   * При денормализации — уметь обосновать решение.

10. **addresses** (Адреса): `id`, `street_address`, `postal_code`, `city_id`.

11. **accommodations** (Объекты): `id`, `owner_id`, `address_id`, `status_id`, `description`, `telegram`.### Acceptance Criteria:

12. **accommodation_details** (Детали): `accommodation_id`, `type_id`, `floor`, `area_sqm`, `bedrooms`, `max_guests`.

13. **accommodation_photos** (Фото): `id`, `accommodation_id`, `url`.1. Разработана даталогическая модель.

14. **exchange_offers** (Предложения): `id`, `from_user_id`, `target_accommodation_id`, `start_date`, `end_date`, `status_id`.2. БД приведена к 3НФ.

15. **reviews_on_accommodations** (Отзывы): `id`, `author_id`, `accommodation_id`, `rating`, `comment`.3. Готовность привести БД к BCNF, 4NF, 5NF, 6NF.

4. Теоретические знания по теме.

#### Связующие таблицы и периоды

16. **accommodation_amenity**: Связь жилья и удобств (M:N).---

17. **role_privileges**: Связь ролей и привилегий (M:N).

18. **accommodation_posted_periods**: Периоды доступности жилья.# Лабораторная работа №3

19. **accommodation_booked_periods**: Занятые периоды.

### Необходимо:

---

1. Разработать **физическую модель** БД (создать БД).

## 📐 Лабораторная работа №2: Даталогическая модель и Нормализация2. Наложить ограничения.

3. Заполнить тестовыми данными.

### Нормализация4. Создать пул SQL-запросов для простых операций.

База данных спроектирована в соответствии с **3-й нормальной формой (3NF)**.

### Acceptance Criteria:

1. **1NF (Первая нормальная форма):**

   - Все атрибуты атомарны.1. Создана физическая модель с ограничениями:

   - Нет повторяющихся групп.

   - У каждой таблицы есть первичный ключ (UUID).   * Все таблицы и связи — **вручную**, без автогенерации.

   * Все SQL-скрипты сохранены.

2. **2NF (Вторая нормальная форма):**   * Добавлены индексы, констрейнты и др.

   - Все таблицы находятся в 1NF.2. БД заполнена тестовыми данными.

   - Все неключевые атрибуты полностью зависят от первичного ключа.3. Составлен пул базовых SQL-запросов.

   - В таблицах со сложным ключом (например, связующие таблицы) нет неключевых атрибутов, зависящих только от части ключа.4. Готовность продемонстрировать работу запросов.

5. Готовность пояснить все операторы SQL.

3. **3NF (Третья нормальная форма):**

   - Все таблицы находятся во 2NF.---

   - Отсутствуют транзитивные зависимости неключевых атрибутов.

   - *Пример:* Адрес (`addresses`) ссылается на город (`cities`), город ссылается на страну (`countries`). В таблице `accommodations` хранится только `address_id`, что исключает дублирование информации о городе и стране и транзитивные зависимости внутри таблицы `accommodations`.# Лабораторная работа №4



### Схема БД### Необходимо:

Физическая схема базы данных представлена в файле `dump.sql` (будет преобразована в миграции в ЛР №3).

1. Создать пул **сложных выборок**.

---2. Пул запросов для создания представлений.

3. Пул запросов по группировкам.

## 🛠 Лабораторная работа №3: Физическая модель (В процессе)4. Пул запросов для сложных операций над данными.

5. Проверить запросы на БД.

*Планируется:*

- Создание Docker контейнера с PostgreSQL.### Acceptance Criteria:

- Написание SQL скриптов инициализации (DDL).

- Заполнение тестовыми данными (DML).1. Сложные выборки:

- Примеры простых запросов.

   * Запросы с несколькими условиями
   * Вложенные запросы
   * Другие необходимые сложные выборки
2. Запросы для представлений, включая:

   * INNER, OUTER, FULL, CROSS, SELF JOIN
3. Запросы для группировок:

   * GROUP BY + агрегаты
   * PARTITION, PARTITION OVER + оконные функции
   * HAVING
   * UNION
4. Запросы сложных операций:

   * EXISTS
   * INSERT INTO SELECT
   * CASE
   * EXPLAIN
5. Не обязательно использовать *все* операторы.
6. Готовность объяснить все использованные операторы.
7. Теоретические знания по теме.

---

# Лабораторная работа №5

### Необходимо:

1. Создать пул триггеров.
2. Создать пул хранимых процедур.
3. Проверить их работу на БД.

### Acceptance Criteria:

1. Создан пул триггеров:

   * Примеры: пересчёт суммы заказа, логирование
   * Триггеры должны реализовывать **логику приложения**
   * Один триггер на логирование — **не засчитывается**
2. Пул хранимых процедур:

   * Процедуры для часто используемых действий
   * Должны храниться в БД и вызываться
3. Демонстрация работы процедур и триггеров.
4. Готовность создать новые.
5. Теоретические знания.

---

# Лабораторная работа №6

### Необходимо:

1. Создать приложение с пользовательским интерфейсом, использующее вашу БД как основную.

### Acceptance Criteria:

1. Приложение имеет пользовательский интерфейс:

   * Консоль: список команд
   * Web/desktop: интерфейс взаимодействия
2. Соответствие ТЗ (из ЛР1).
3. Используется БД, созданная в ЛР 1–5.
4. Взаимодействие с БД производится одним из способов:

   * Прямые SQL-запросы
   * Самописная ORM
   * ORM.raw_sql() или аналоги
     (**важно:** работа исключительно через SQL)
5. Используются запросы из ЛР 3–5.
6. Готовность редактировать код при сдаче.
7. Теоретические знания.
