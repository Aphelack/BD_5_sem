# Проект "HomeFlip" - Сервис обмена жильем

**Студент:** Кореник Илья
**Группа:** 353502

---

## 🤖 Copilot Instructions & Plan

Этот раздел предназначен для отслеживания прогресса выполнения лабораторных работ и инструкций для будущих сессий.

### Текущий статус
- [x] **Lab 1:** Определение темы, требований и сущностей.
- [x] **Lab 2:** Нормализация и даталогическая модель.
- [ ] **Lab 3:** Физическая модель, миграции, Docker, тестовые данные.
- [ ] **Lab 4:** Сложные выборки и представления.
- [ ] **Lab 5:** Триггеры и хранимые процедуры.
- [ ] **Lab 6:** Приложение с UI.

### Инструкции
1. При выполнении следующей лабораторной работы, обновляйте соответствующий раздел в этом файле.
2. Все SQL скрипты должны быть идемпотентными (по возможности) и находиться в папке `scripts/`.
3. Поддерживайте актуальность `docker-compose.yml`.

---

## 📝 Лабораторная работа №1: Инфологическая модель

### Тема проекта
**HomeFlip** — сервис для обмена жильем (Home Exchange). Позволяет пользователям находить варианты для проживания в других городах и странах, предлагая взамен свое жилье.

### Функциональные требования
Подробные функциональные требования описаны в файле [functional_requirements.md](functional_requirements.md).
Основные возможности:
- Регистрация и аутентификация пользователей.
- Управление профилем и ролями.
- CRUD операции для объектов недвижимости (Accommodations).
- Поиск и фильтрация жилья.
- Система бронирования (Exchange Offers).
- Отзывы и рейтинги.

### Описание сущностей (ER-диаграмма)

#### Справочники
1. **countries** (Страны): `id`, `name`, `iso_code`.
2. **cities** (Города): `id`, `name`, `country_id`, `region`.
3. **accommodation_types** (Типы жилья): `id`, `name` (Квартира, Дом, Лофт...), `description`.
4. **accommodation_statuses** (Статусы жилья): `id`, `code` (draft, published, archived), `name`.
5. **exchange_offer_statuses** (Статусы обмена): `id`, `code` (pending, accepted, rejected), `name`.
6. **amenities** (Удобства): `id`, `short_name` (Wi-Fi, Кухня, Бассейн...).

#### Основные сущности
7. **users** (Пользователи): `id`, `email`, `yandex_id`, `first_name`, `last_name`, `phone`, `role_id`.
8. **roles** (Роли): `id`, `name` (admin, user, guest), `description`.
9. **privileges** (Привилегии): `id`, `name`, `description`.
10. **addresses** (Адреса): `id`, `street_address`, `postal_code`, `city_id`.
11. **accommodations** (Объекты): `id`, `owner_id`, `address_id`, `status_id`, `description`, `telegram`.
12. **accommodation_details** (Детали): `accommodation_id`, `type_id`, `floor`, `area_sqm`, `bedrooms`, `max_guests`.
13. **accommodation_photos** (Фото): `id`, `accommodation_id`, `url`.
14. **exchange_offers** (Предложения): `id`, `from_user_id`, `target_accommodation_id`, `start_date`, `end_date`, `status_id`.
15. **reviews_on_accommodations** (Отзывы): `id`, `author_id`, `accommodation_id`, `rating`, `comment`.

#### Связующие таблицы и периоды
16. **accommodation_amenity**: Связь жилья и удобств (M:N).
17. **role_privileges**: Связь ролей и привилегий (M:N).
18. **accommodation_posted_periods**: Периоды доступности жилья.
19. **accommodation_booked_periods**: Занятые периоды.

---

## 📐 Лабораторная работа №2: Даталогическая модель и Нормализация

### Нормализация
База данных спроектирована в соответствии с **3-й нормальной формой (3NF)**.

1. **1NF (Первая нормальная форма):**
   - Все атрибуты атомарны.
   - Нет повторяющихся групп.
   - У каждой таблицы есть первичный ключ (UUID).

2. **2NF (Вторая нормальная форма):**
   - Все таблицы находятся в 1NF.
   - Все неключевые атрибуты полностью зависят от первичного ключа.
   - В таблицах со сложным ключом (например, связующие таблицы) нет неключевых атрибутов, зависящих только от части ключа.

3. **3NF (Третья нормальная форма):**
   - Все таблицы находятся во 2NF.
   - Отсутствуют транзитивные зависимости неключевых атрибутов.
   - *Пример:* Адрес (`addresses`) ссылается на город (`cities`), город ссылается на страну (`countries`). В таблице `accommodations` хранится только `address_id`, что исключает дублирование информации о городе и стране и транзитивные зависимости внутри таблицы `accommodations`.

### Схема БД
Физическая схема базы данных представлена в файле `dump.sql` (будет преобразована в миграции в ЛР №3).

---

## 🛠 Лабораторная работа №3: Физическая модель (В процессе)

*Планируется:*
- Создание Docker контейнера с PostgreSQL.
- Написание SQL скриптов инициализации (DDL).
- Заполнение тестовыми данными (DML).
- Примеры простых запросов.
